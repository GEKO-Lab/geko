## Portfolio (React + Three.js)

Dieses Repo enthält ein Portfolio mit **Three.js/R3F Live-Hintergrund** (Gradient-Himmel, Wasser, Wasserfall) und **Scroll-Parallax**.

### Lokales Starten

```bash
cd web
npm install
npm run dev
```

### Build

```bash
cd web
npm run build
```
